# apt-install-services.yml
---
- hosts: routers
  become: true
  tasks:
  - name: Install Quagga, Quagga-docs and Pip3
    yum:
      name:
        - quagga
        - quagga-doc
        - python-pip3
      state: present
  # - name: Install pytest
  #   pip:
  #     name: pytest
  #     state: present
  #     executable: pip3